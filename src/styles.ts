// styles.ts
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  profilePicture: {
    width: 100,
    height: 100,
    borderRadius: 50,
  },
});

export default styles;
